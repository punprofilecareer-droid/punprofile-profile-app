import { defineSchema } from "convex/server";

// Real tables (leads, magicLinks) land in Phase 1 — TASK-011.
// Kept empty for now so `npx convex dev` starts cleanly once a project is linked.
export default defineSchema({});
