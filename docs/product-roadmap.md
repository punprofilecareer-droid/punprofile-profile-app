# Product Roadmap — EU Fit Check

> Generated by the Product Planner skill. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 4/50 tasks complete
**Current Phase:** Phase 0 — Foundation & Setup (TASK-002/003/004/007/009/010 blocked on Convex/Sentry/Vercel account setup — see below)

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** Phase 0 sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The teaser spider-chart reveal — the core value proposition — works as early as possible, by the end of Phase 1. Everything else builds on top.
4. **Test as you go.** Don't save testing for the end. Each task includes verification.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Honesty over conversion.** Never let a task's implementation quietly make the self-report chart look more authoritative than the data behind it, or move a contact-info ask earlier than the teaser reveal — these are product-integrity rules, not just UX preferences.
8. **Consent is data-model work, not a UI afterthought.** PDPA consent capture belongs in the data layer alongside the field it covers, done in the same task, never bolted on later.

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD and vision doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything:** `docs/prd.md` is long. Each phase's Reference sections tell the agent exactly what to read.
- **Don't skip tasks:** Tasks are ordered intentionally — later tasks assume earlier ones are done.
- **Verify after each phase:** Run the app after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let a review agent check it. Don't start the next phase until the PR is merged.
- **Update the status line:** After completing tasks, update the header: `**Status:** X/50 tasks complete` and `**Current Phase:** Phase N`.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md and docs/product-vision.md (and docs/design.md once it exists). Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called phase-{N}/{slug}, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."

---

## Phase 0: Foundation & Setup

> **Goal:** A running Next.js + Convex app shell, deployed to Vercel, with error tracking live from day one and a single working admin login — no candidate features yet, but everything later work builds on top of a verified, working foundation.

**Reference sections — read these before starting this phase:**
- PRD: § 2 Technical Architecture (all subsections), § 9 Auth Implementation
- Vision: `docs/VISION.md` § Tech Stack

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read PRD § 2 Technical Architecture and § 9 Auth Implementation from docs/prd.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-setup`, commit, push, and open a PR for review."

- [x] **TASK-001** — Scaffold the Next.js project with App Router, TypeScript, and Tailwind CSS.
  Files: `package.json`, `tsconfig.json`, `tailwind.config.ts`, `src/app/layout.tsx`, `src/app/page.tsx`
  Notes: Use `npx create-next-app@latest` with App Router + TypeScript + Tailwind selected. Verify: `npm run dev` serves a default page at `localhost:3000`.

- [ ] **TASK-002** — Install and initialize Convex; verify the dev connection.
  Files: `convex/`, `.env.local`
  Notes: `npm install convex`, run `npx convex dev` to scaffold `convex/` and obtain `NEXT_PUBLIC_CONVEX_URL`. Add a placeholder `convex/schema.ts` (empty `defineSchema({})`) so the dev server starts cleanly. Verify: Convex dashboard shows the dev deployment connected.

- [ ] **TASK-003** — Wire the Convex client provider into the Next.js app.
  Files: `src/app/providers.tsx`, `src/app/layout.tsx`
  Notes: Wrap the root layout in a `ConvexProvider` using `NEXT_PUBLIC_CONVEX_URL`. Verify: a simple test query round-trips from a page component without errors.

- [ ] **TASK-004** — Install and configure Convex Auth for a single admin account.
  Files: `convex/auth.config.ts`, `convex/auth.ts`, `.env.local`
  Notes: Follow `@convex-dev/auth`'s Next.js App Router setup. Provision exactly one admin identity from the `ADMIN_EMAIL` environment variable — no public sign-up route. Per PRD § 9, do not share any session/cookie namespace with the future candidate magic-link mechanism. Verify: an admin can log in via a temporary bare-bones login page.

- [x] **TASK-005** — Build the base app shell and root layout.
  Files: `src/app/layout.tsx`, `src/components/ui/` (placeholder primitives)
  Notes: Minimal header/footer, no visual styling decisions yet beyond Tailwind defaults — real tokens come once `docs/design.md` exists (see TASK-006). Verify: layout renders on both a candidate-facing route and an admin route without console errors.

- [x] **TASK-006** — Flag the missing design system and use temporary placeholder styling.
  Files: `docs/design.md` (does not exist yet — do not create it here), `tailwind.config.ts`
  Notes: `docs/design.md` has not been generated. Use plain Tailwind defaults as a placeholder (no custom color palette yet) and leave a comment in `tailwind.config.ts` noting that real design tokens should replace these once the Design System skill has been run. Do not invent a color palette or typography scale — that's out of scope for this task.
  Update: Tailwind v4 (this scaffold) has no `tailwind.config.ts` by default — the placeholder-flag comment lives in `src/app/globals.css` instead, next to the `@theme inline` tokens it refers to.

- [ ] **TASK-007** — Initialize Sentry for client, server, and edge.
  Files: `sentry.client.config.ts`, `sentry.server.config.ts`, `sentry.edge.config.ts`, `next.config.ts`
  Notes: Run `npx @sentry/wizard@latest -i nextjs`. Per PRD § 2 Security Considerations, add a `beforeSend` hook stripping any request body/breadcrumb fields matching email/phone/LINE ID patterns — do this now so no PII-leaking error is ever captured, even during later development. Verify: trigger a test error and confirm it appears in the Sentry dashboard with no PII in the payload.

- [x] **TASK-008** — Document all required environment variables.
  Files: `.env.example`, `README.md`
  Notes: List `NEXT_PUBLIC_CONVEX_URL`, `CONVEX_DEPLOY_KEY`, `RESEND_API_KEY` (Convex env, not Next.js), `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `ADMIN_EMAIL`. Verify: a fresh clone can follow `README.md` to get a working local dev environment.

- [ ] **TASK-009** — Connect the repository to Vercel and configure the build command.
  Files: `vercel.json` (if needed), `package.json` (build script)
  Notes: Set the build command to run `npx convex deploy` before `next build` so schema/function changes ship with frontend changes (per PRD § 2 Infrastructure & Deployment). Add all environment variables from TASK-008 to the Vercel project settings. Verify: a push to main triggers a successful Vercel deployment.

- [ ] **TASK-010** — End-to-end foundation verification.
  Files: none (verification task)
  Notes: Confirm locally and on the deployed Vercel preview: app loads with no console errors, a test Convex query works, the placeholder admin login works, and a deliberately-triggered test error reaches Sentry. This is the "foundation is solid" checkpoint before any feature work begins.

---

## Phase 1: Core Self-Assessment Flow (Magic Moment)

> **Goal:** A candidate can start an anonymous assessment on their phone, answer the pathway question plus a first short set of questions, and see a partial self-report spider chart render — before being asked for any contact information. This is the magic moment; it must work reliably by the end of this phase.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model, § 4 API Specification, § 5 User Stories (Epic: Progressive Self-Assessment), § 6 Functional Requirements (FR-001 through FR-004, FR-007, FR-008), § 8 UI/UX Requirements (Screen: Landing / Entry, Screen: Progressive Assessment)
- Vision: `docs/product-vision.md` § 3 Product Strategy → Magic Moment Design, MVP Definition

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read PRD § 3 Data Model, § 4 API Specification, § 5 User Stories (Progressive Self-Assessment epic), § 6 FR-001–FR-004/FR-007/FR-008, and § 8 (Landing and Progressive Assessment screens) from docs/prd.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/core-self-assessment-flow`, commit, push, and open a PR for review."

- [x] **TASK-011** — Define the `leads` and `magicLinks` Convex schema.
  Files: `convex/schema.ts`
  Notes: Implement exactly the shape specified in PRD § 3 Data Model, including all indexes (`by_email`, `by_status_recency`, `by_pathway`, `by_token`, `by_lead`). Verify: `npx convex dev` accepts the schema with no type errors.

- [ ] **TASK-012** — Implement `leads.startSession` mutation.
  Files: `convex/leads.ts`
  Notes: Creates a new `leads` document with `status: "partial"`, `createdAt`/`updatedAt`/`lastActivityAt` set to now, and an optional `source` argument. Per PRD § 4. Verify: calling this from a Convex dashboard test creates a document visible in the data browser.

- [ ] **TASK-013** — Implement `leads.setPathway` mutation.
  Files: `convex/leads.ts`
  Notes: Sets the `pathway` field to one of the four literal values. Per FR-002 — all four options must be treated with equal weight in the data model (no default value). Verify: setting each of the four pathway values persists correctly.

- [x] **TASK-014** — Implement the initial self-report scoring logic.
  Files: `convex/scoring.ts`
  Notes: Given a partial or complete `responses` record, compute whichever of `professionalCapability`, `employability`, `mobilityReadiness`, `europeanMarketFit` have enough answered questions to produce a score, leaving the rest `undefined`. Start with a simple, clearly-documented placeholder scoring formula (e.g. an average of relevant question values per dimension) — the exact question-to-dimension mapping and weights are flagged as an open question in PRD § 14 and will need a content pass; this task's job is the mechanism, not the final formula. Verify: unit-test (or manually test via Convex dashboard) that partial responses produce partial scores without errors.

- [ ] **TASK-015** — Implement `leads.submitAnswer` mutation, wired to scoring.
  Files: `convex/leads.ts`, `convex/scoring.ts`
  Notes: Merges one answer into `responses`, calls the scoring logic from TASK-014, updates `scores` and `lastActivityAt`. Per PRD § 4. Verify: submitting an answer updates both `responses` and `scores` in the same write.

- [ ] **TASK-016** — Implement `leads.getSession` reactive query.
  Files: `convex/leads.ts`
  Notes: Returns the current lead's `responses`, `scores`, `status`, and `pathway` for the in-progress assessment UI (excluding internal-only fields). Verify: a test client subscription re-renders automatically after `submitAnswer` runs.

- [ ] **TASK-017** — Draft the first-question-set content (pathway + short readiness questions), Thai-first.
  Files: `src/lib/questions.ts` (or similar content module)
  Notes: Keep the pre-teaser question set under 10 questions total including the pathway question, per PRD § 1 Magic Moment. Follow `03_Content_System.md`'s tone (Clear, Honest, Supportive, Educational, Optimistic without unrealistic expectations) — Thai-first, natural conversational register, not literal-translated corporate English. Flag this content as a first draft; PRD § 14 notes a fuller content pass is still needed. Verify: content reads naturally to a Thai speaker, not machine-translated.

- [ ] **TASK-018** — Build the landing / entry screen.
  Files: `src/app/page.tsx`
  Notes: Per PRD § 8 Screen: Landing / Entry. Single-column, mobile-first, one primary CTA calling `leads.startSession` then navigating to `/assess/1`. Verify: loads correctly on a 375px-wide viewport with no horizontal scroll.

- [ ] **TASK-019** — Build the pathway-selector component.
  Files: `src/components/features/assessment/PathwaySelector.tsx`
  Notes: Four equally-weighted options (job-first / study-first / family / not sure), tap-based, no free text. Per FR-002. Verify: selecting any option calls `leads.setPathway` and advances the flow.

- [ ] **TASK-020** — Build the progressive question-flow screen.
  Files: `src/app/assess/[step]/page.tsx`, `src/components/features/assessment/QuestionCard.tsx`
  Notes: One question (or small tightly-related group) per screen, tap/select-based, mobile-first, per PRD § 8 Screen: Progressive Assessment. Each answer calls `leads.submitAnswer`. Use optimistic UI updates so a selected answer never visibly disappears while the mutation is in flight (per § 11 Edge Cases). Verify: full click-through of the first question set works on a real mobile device or emulator.

- [ ] **TASK-021** — Build the teaser spider-chart component.
  Files: `src/components/features/chart/SpiderChart.tsx`
  Notes: Renders using `recharts` (or equivalent), must render meaningfully with as few as 1 of 4 dimensions populated, showing unpopulated dimensions as "not yet answered" rather than zero. Must include the persistent "self-reported, preliminary" label per FR-007. Verify: renders correctly with 1, 2, 3, and 4 dimensions populated.

- [ ] **TASK-022** — Wire the teaser chart into the assessment flow.
  Files: `src/app/assess/[step]/page.tsx`
  Notes: Once the first question set is complete, render the `SpiderChart` teaser variant inline, subscribed reactively to `leads.getSession` so it updates live as more answers arrive. Confirm no email/phone/LINE ID field renders anywhere on screen at this point, per FR-004. Verify: time from landing to first chart render is under 60 seconds at a normal answering pace (§ 1 Success Criteria).

---

## Phase 2: Lead Capture, Magic-Link Resume & Coach Dashboard

> **Goal:** A candidate can unlock their full self-report profile with email (with PDPA consent), optionally add phone/LINE ID, return later via a magic link, and Paul can log in and triage every captured lead from a dashboard.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model, § 4 API Specification, § 5 User Stories (Contact Capture & Consent, Coach Lead Dashboard epics), § 6 Functional Requirements (FR-005, FR-006, FR-009 through FR-014), § 8 UI/UX Requirements (Screen: Email Capture, Full Result, Magic-Link Resume, Admin Login, Admin Dashboard, Admin Lead Detail), § 9 Auth Implementation
- Vision: `docs/product-vision.md` § 4 Brand Strategy → Voice & Tone Guide (for consent/result copy)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read PRD § 3, § 4, § 5 (Contact Capture & Consent and Coach Lead Dashboard epics), § 6 FR-005/FR-006/FR-009–FR-014, § 8 (Email Capture, Full Result, Magic-Link Resume, Admin screens), and § 9 from docs/prd.md, plus product-vision.md § 4 Voice & Tone Guide. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/lead-capture-and-dashboard`, commit, push, and open a PR for review."

- [ ] **TASK-023** — Write centralized, Thai-first PDPA consent copy.
  Files: `src/lib/consent-copy.ts`
  Notes: Separate consent strings for email, phone, and LINE ID, following the tone guide in `product-vision.md` § 4. Explicitly flag in a code comment that this copy is a first draft pending PDPA legal review, per PRD § 14 Open Questions — do not treat this task as resolving that open question.

- [ ] **TASK-024** — Implement secure magic-link token generation.
  Files: `convex/leads.ts`
  Notes: Generate a cryptographically random token (128+ bits) on email capture, store it in `magicLinks` with a 30-day `expiresAt`, and ensure only the most recent token per lead is treated as valid going forward (per PRD § 3 Relationships). Verify: two consecutive token generations for the same lead — only the newest resolves via `by_token` lookup logic in later tasks.

- [ ] **TASK-025** — Implement `leads.captureEmail` mutation.
  Files: `convex/leads.ts`
  Notes: Validates email format server-side, requires `consentGranted: true`, sets `email` and `emailConsentAt`, transitions `status` to `"email_captured"`, and triggers the token generation from TASK-024 plus the `email.sendMagicLink` action. Per FR-005, FR-006. Verify: capturing without consent is rejected server-side even if a client bypasses the UI check.

- [ ] **TASK-026** — Implement Resend email actions.
  Files: `convex/email.ts`
  Notes: `email.sendMagicLink` (candidate-facing, Thai-first copy, includes the resume link) and `email.notifyNewLead` (to `ADMIN_EMAIL`, summarizing pathway + link to the admin detail view). Per PRD § 4, § 12. Use Convex's action retry pattern so a failed send doesn't silently drop — per § 11 Edge Cases. Verify: both emails send successfully in a Resend sandbox/test mode.

- [ ] **TASK-027** — Build the email-capture (unlock) screen.
  Files: `src/components/features/assessment/EmailCaptureStep.tsx`
  Notes: Embedded step within the assessment flow per PRD § 8 Screen: Email Capture. Use `react-hook-form` + `zod` for validation. Keep the teaser chart visible/partially visible above the form. Verify: malformed email shows inline error without calling the mutation; valid submission unlocks the full result.

- [ ] **TASK-028** — Build the full result screen.
  Files: `src/app/result/page.tsx`, `src/components/features/chart/SpiderChart.tsx` (full variant)
  Notes: Per PRD § 8 Screen: Full Result and FR-007/FR-008. Full 4-dimension chart, persistent "self-reported, preliminary" label, pathway-aware narrative text (first sentence must differ meaningfully by pathway per FR-008). Verify: narrative text visibly changes across all four pathway values.

- [ ] **TASK-029** — Implement `leads.captureContact` mutation and its UI.
  Files: `convex/leads.ts`, `src/components/features/assessment/ContactCaptureForm.tsx`
  Notes: Phone and LINE ID are each independently optional, each with its own consent flag/timestamp per FR-009/FR-006. Renders on the result screen, clearly optional, skippable without losing access to the result. Verify: submitting only phone, only LINE ID, and both, all work independently.

- [ ] **TASK-030** — Build the magic-link resume route.
  Files: `src/app/resume/[token]/page.tsx`
  Notes: Calls `leads.getByMagicLinkToken`, restores exact prior `responses`/`scores`/`status`, redirects into the correct in-flow screen. Handles expired/invalid tokens per PRD § 8 Screen: Magic-Link Resume and § 11 Edge Cases (generic "link not found" message, no internal-state leakage). Verify: an expired token shows the recovery message; a valid token restores state exactly.

- [ ] **TASK-031** — Build the "request a new link" recovery flow.
  Files: `src/app/resume/[token]/page.tsx` (or a dedicated recovery component)
  Notes: On expired/invalid token, let the candidate re-enter their email; look up via `by_email` and re-trigger `email.sendMagicLink` if found. Verify: re-requesting sends a fresh, working link.

- [ ] **TASK-032** — Implement `leads.listForAdmin` and `leads.getForAdmin` queries with server-side admin checks.
  Files: `convex/leads.ts`
  Notes: Both must verify the caller's Convex Auth identity matches `ADMIN_EMAIL` and throw otherwise — per PRD § 2 Security Considerations, this cannot be a UI-only check. `listForAdmin` excludes `status: "partial"` leads and supports an optional status filter, sorted by `by_status_recency`. Verify: a non-admin identity (or no identity) calling either query is rejected server-side.

- [ ] **TASK-033** — Build the admin login screen and protected-route middleware.
  Files: `src/app/admin/login/page.tsx`, `src/middleware.ts` (or layout-level check)
  Notes: Per PRD § 8 Screen: Admin Login and § 9 Protected Routes. All `/admin/*` routes except `/admin/login` redirect to login if no valid Convex Auth session exists. Verify: visiting `/admin/dashboard` while logged out redirects to `/admin/login`.

- [ ] **TASK-034** — Build the admin dashboard screen.
  Files: `src/app/admin/dashboard/page.tsx`, `src/components/features/dashboard/LeadList.tsx`
  Notes: Per PRD § 8 Screen: Admin Dashboard and FR-012. Recency-sorted list, pathway/status/contact-completeness/consent visible without opening a row, reactive live updates, status filter control, explicit empty state. Verify: a newly-captured lead appears in the list live without a manual refresh.

- [ ] **TASK-035** — Build the admin lead detail screen.
  Files: `src/app/admin/leads/[id]/page.tsx`, `src/components/features/dashboard/LeadDetail.tsx`
  Notes: Per PRD § 8 Screen: Admin Lead Detail and FR-013. Full chart, pathway, all raw responses, every consent timestamp; missing data shown explicitly as "not provided." Verify: clicking a row from the dashboard opens the correct detail view with all available data visible.

- [ ] **TASK-036** — End-to-end Phase 2 verification.
  Files: none (verification task)
  Notes: Walk through the full candidate flow (start → teaser → email capture → full result → optional phone/LINE ID) on a real mobile device, confirm the lead appears correctly in the admin dashboard, open its detail view, then verify a magic-link resume restores state correctly from a different browser/session.

---

## Phase 3: Polish, Compliance & Launch Prep

> **Goal:** The app is production-ready — hardened against real-world edge cases, instrumented for analytics and error tracking, accessible, performant on mobile, and the Google Form has been fully retired in favor of EU Fit Check across every existing traffic source.

**Reference sections — read these before starting this phase:**
- PRD: § 7 Non-Functional Requirements, § 11 Edge Cases & Error Handling, § 14 Open Questions
- Vision: `docs/product-vision.md` § 3 Product Strategy → Success Metrics, Risks

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read PRD § 7 Non-Functional Requirements, § 11 Edge Cases & Error Handling, and § 14 Open Questions from docs/prd.md, plus product-vision.md § 3 Success Metrics and Risks. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/polish-and-launch-prep`, commit, push, and open a PR for review."

- [ ] **TASK-037** — Implement comprehensive edge-case handling across the candidate flow.
  Files: various (`convex/leads.ts`, assessment components)
  Notes: Cover every row of PRD § 11 Edge Cases & Error Handling for Progressive Assessment, Email Capture & Consent, and Magic-Link Resume — network-drop retries, duplicate-submit debouncing, malformed email, unchecked consent, expired/tampered tokens. Verify against each table row individually.

- [ ] **TASK-038** — Implement duplicate-email detection and manual-review flagging.
  Files: `convex/leads.ts`
  Notes: When `captureEmail` matches an existing lead via `by_email`, flag the new session for manual review rather than auto-merging (per PRD § 11 and § 14 Open Questions recommended default). Surface this flag in the admin dashboard. Verify: starting a second session with an email already on file produces a visibly-flagged lead, not a silent duplicate or merge.

- [ ] **TASK-039** — Add rate limiting to session-creation and email-capture endpoints.
  Files: `convex/leads.ts`
  Notes: Simple per-IP or per-session throttle guard on `leads.startSession` and `leads.captureEmail`, per PRD § 7 Security and § 14 Open Questions recommended default. Verify: rapid repeated calls from the same source are throttled without blocking normal usage.

- [ ] **TASK-040** — Install and instrument PostHog across the funnel.
  Files: `src/lib/posthog.ts`, `src/app/providers.tsx`, assessment/result/dashboard components
  Notes: Fire events at session start, pathway selected, teaser shown, email captured, contact captured, and assessment completed, per FR-015. Include `status` and `pathway` as event properties; explicitly exclude email/phone/LINE ID from any event payload. Configure `person_profiles: 'identified_only'` and disable autocapture of form input values, per PRD § 2 Security Considerations. Verify: events appear in the PostHog dashboard with no PII fields present.

- [ ] **TASK-041** — Verify and harden Sentry PII scrubbing.
  Files: `sentry.client.config.ts`, `sentry.server.config.ts`
  Notes: Confirm the `beforeSend` hook from TASK-007 correctly strips email/phone/LINE ID from real assessment-flow errors, not just synthetic test errors. Verify: deliberately trigger an error from within a form containing PII and confirm the captured Sentry event has it scrubbed.

- [ ] **TASK-042** — Accessibility pass (WCAG 2.1 AA).
  Files: various (chart, form, dashboard components)
  Notes: Keyboard-navigate the entire candidate flow and admin dashboard; add a text-summary fallback for the spider chart for screen-reader users, per PRD § 7 Accessibility. Verify: full flow completable using keyboard only; chart data available in non-visual form.

- [ ] **TASK-043** — Mobile responsiveness and real-device pass.
  Files: various
  Notes: Test on at least one real Android and one real iOS device (or accurate emulators), not just desktop responsive breakpoints, given this audience is disproportionately mobile-only per `product-vision.md`. Verify: no horizontal scroll, no cut-off tap targets, teaser chart legible at small sizes.

- [ ] **TASK-044** — Performance verification against PRD § 7 thresholds.
  Files: various
  Notes: Measure LCP under 3G-equivalent throttling (target < 2.5s), confirm initial candidate-facing JS bundle is under 250KB gzipped, confirm Convex query/mutation round-trip stays under 300ms p95 in manual testing. Verify: record actual measured numbers against each threshold; address any that fail before proceeding.

- [ ] **TASK-045** — Graceful degradation check for third-party service outages.
  Files: various
  Notes: Confirm the core assessment flow still works if PostHog or Sentry fail to load (neither should be a hard dependency), per PRD § 7 Reliability. Verify: block PostHog/Sentry network requests in devtools and confirm the assessment flow is unaffected.

- [ ] **TASK-046** — Wire the discovery-call CTA to PunProfile's actual booking mechanism.
  Files: `src/app/result/page.tsx`
  Notes: Per PRD § 14 Open Questions — confirm with the founder what the CTA should link to (existing scheduling link, DM prompt, etc.) before finalizing; do not build a new scheduling system. Verify: the CTA on the result screen links to the confirmed destination.

- [ ] **TASK-047** — PDPA consent-copy legal review checkpoint.
  Files: `src/lib/consent-copy.ts`
  Notes: This is a checklist/flag task, not an automated one — confirm with the founder whether the placeholder consent copy from TASK-023 has been reviewed against current Thai PDPA guidance before launch, per PRD § 14 Open Questions. Do not mark this complete without an explicit founder confirmation.

- [ ] **TASK-048** — Retire the Google Form across all existing traffic sources.
  Files: none (content/ops task, tracked here for completeness)
  Notes: Per the founder's decision (product-vision.md § Business), the Form is retired the moment EU Fit Check ships — no parallel run. Update the Free Consultation Hook link and the pinned Facebook Group post to point to EU Fit Check's production URL. This is an operational task in the `punprofile-career-coaching` repo's content, not this codebase — flag it as a coordinated launch step, not something the coding agent can complete alone.

- [ ] **TASK-049** — Final QA pass against PRD § 1 Success Criteria.
  Files: none (verification task)
  Notes: Re-verify all § 1 Success Criteria explicitly: teaser render < 60s, teaser works with 1/4 dimensions populated, LCP < 2.5s on 3G, all P0 functional requirements implemented and manually verified on both mobile and desktop, zero contact-info fields shown before first teaser render.

- [ ] **TASK-050** — Production deployment and smoke test.
  Files: none (deployment task)
  Notes: Deploy to the production Vercel environment, run `npx convex deploy --prod` (or equivalent production deploy), and manually smoke-test the full candidate flow plus admin login on the live production URL before announcing launch.

---

## Addendum, 04/08/2026: reality check against build state

The scoring engine, report renderer and candidate data architecture were built
offline against the 63 real Lead Discovery Survey responses before Phase 1
started. That moves several task notes out of date:

- **TASK-014 exceeded its spec and is checked above.** The roadmap asked for a
  placeholder averaging formula; what exists is the full three-tier mapping
  (`src/lib/scoring.ts`, spec in `docs/self-report-scoring.md`), validated
  against all 63 real responses. The PRD § 14 open question on scoring weights
  is resolved.
- **TASK-021: the recharts note is obsolete.** The chart is a dependency-free
  SVG builder (`src/lib/radar.ts`) shared by the app, the offline reports and
  future result emails. Partial-data rendering and "not yet answered" hollow
  markers per FR-007 already exist. Remaining work: React wrapper, teaser
  sizing, wiring.
- **TASK-028 is mostly banked.** Narrative, labelling and the full chart exist
  (`src/lib/narrative.ts`, `report.ts`); the task reduces to the app screen.
  The pathway-aware first sentence per FR-008 does NOT exist yet, see TASK-051.
- **TASK-033 was largely delivered inside TASK-004.** Middleware protection and
  the login screen are live; the login route is `/login`, not `/admin/login`.
- **Candidate-facing views:** `docs/candidate-data-architecture.md` added a
  coach playbook and candidate journey layer (`src/lib/levers.ts`, `views.ts`)
  the original roadmap never anticipated. The result screen should render the
  candidate journey, not only the chart.

New tasks:

- [ ] **TASK-051**, map the app's short question set onto `ScoringInput` and add
  pathway-aware narrative variants (FR-008). The survey-era parsers expect the
  Google Form vocabulary; the app asks tap-only questions, so each app question
  needs an explicit mapping to a `ScoringInput` field, and the four pathways
  need distinct opening narrative lines. Depends on TASK-017. The highest-signal
  questions are already identified in `docs/self-report-scoring.md`: visa,
  English level, AI habits, family, CV status, LinkedIn status, plus pathway and
  target country/role (use the Job Title Pool from `08_Coaching_Business.md` as
  the role picker, no free text).
- [ ] **TASK-052**, one consolidated Thai native-tone pass over ALL candidate
  copy (questions, narrative, journey, consent, emails) against
  `03_Content_System.md`, instead of the per-task passes scattered above.
  Requires the founder's native read; nothing candidate-facing ships before it.
- [ ] **TASK-053**, backfill the 63 historical survey leads into Convex as
  `assessments` rows with `source: "survey_import"` via the existing import
  pipeline (`scripts/import-sheet.ts`). The dashboard then has real data on day
  one, and the re-engagement play (send each historical lead their result plus
  discovery-call CTA) becomes a query instead of a project. Do after TASK-032.
- [ ] **TASK-054**, funnel cutover spec for TASK-048: what happens to the
  6-question quiz (retire alongside the Form, or repoint at the app), which
  links change (pinned post, Free Consultation Hook, `00_Quick_Facts.md`), and
  in which order. Decide with the coaching repo's owner docs open; the quiz and
  survey are distinct instruments (see root `CLAUDE.md`, four instruments).
